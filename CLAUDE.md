# NKT Rescue — ระบบบันทึก EMS และการส่งต่อผู้ป่วย

ER โรงพยาบาลสมเด็จพระยุพราชนครไทย · Next.js 16 + Supabase · deploy บน Vercel

เขียนใหม่แทนระบบ Google Apps Script + Google Sheets เดิม ฟิลด์ทุกช่องถอดมาจาก
ทะเบียนของจริง ไม่ได้ออกแบบขึ้นเอง — **ก่อนแก้ฟอร์มหรือฟอร์มพิมพ์ ให้อ่าน
`docs/legacy-app-spec.md` (หน้าจอ) และ `docs/legacy-backend-spec.md` (ข้อมูล) ก่อน**

เริ่มต้นใช้งาน/ส่งมอบ: `docs/handover.md`

## กติกาสำคัญ

- **ห้ามให้เบราว์เซอร์แตะฐานข้อมูลตรงๆ** — schema เปิด RLS แต่ตั้งใจไม่มี policy
  ให้ `anon` เลย ทุกอย่างผ่าน server component / server action ที่ใช้
  `db()` จาก `src/lib/supabase/admin.ts` (service_role)
  ห้าม import ไฟล์นั้นจากไฟล์ที่มี `"use client"`
- **server action ทุกตัวต้องเช็ก session เอง** — ยิง POST ตรงเข้ามาได้ ไม่ได้ผ่านหน้าจอเสมอ
- **ค่าที่ผู้ใช้เลือกได้ต้องตรงกับ check constraint** ใน `supabase/v2/0001_init.sql`
  ตัวเลือกทั้งหมดอยู่ที่ `src/lib/domain/options.ts` ที่เดียว แก้ทั้งสองที่ให้ตรงกัน
- **เวลาไทยเสมอ** — เซิร์ฟเวอร์รันเป็น UTC ใช้ helper ใน `src/lib/domain/datetime.ts`
  อย่าเรียก `new Date()` ตรงๆ เพื่อหาว่า "วันนี้" คือวันไหน
- **ลายเซ็นญาติและรูปถ่ายยืนยัน** เก็บใน Storage bucket ส่วนตัว เก็บแค่ path ในตาราง
  แสดงผลด้วยลิงก์มีอายุ (`signAttachments`) — ห้ามทำ bucket เป็น public
- **เลข "เหตุที่ N"** ออกจากฐานข้อมูล (`next_ems_seq`) + unique (วันที่, ลำดับ)
  อย่ากลับไปนับจำนวนแถวแบบระบบเดิม เพราะลบเคสแล้วเลขจะซ้ำ

## จุดที่ผิดง่าย

- ฟอร์ม ALS ต้องลง A4 หน้าเดียว — ขนาดตัวอักษร/ระยะใน `src/components/print/alsCss.ts`
  ปรับมาพอดีแล้ว แตะแล้วล้นหน้า
- "ไม่พบเหตุ" ล็อกทุก section ตั้งแต่ข้อมูลผู้ป่วยลงไป
- "เสียชีวิต ณ จุดเกิดเหตุ" บังคับ RC Code เป็นสีดำในฟอร์มพิมพ์ ทับระดับที่เลือกไว้
- ชีตเก่าบางเดือนมี 50 คอลัมน์ (ไม่มีช่อง DX) คอลัมน์หลังจากนั้นเลื่อน 1 ช่อง
  — ตัวนำเข้าจัดการให้แล้ว อย่าแก้ให้เลื่อนทื่อๆ

## คำสั่งที่ใช้บ่อย

```bash
npm run dev      # รันที่ localhost:3000 (ต่อฐานข้อมูลจริง)
npm run build    # ตรวจ type + build ให้ผ่านก่อน push เสมอ
npx eslint src   # ตรวจ lint
```

@AGENTS.md
